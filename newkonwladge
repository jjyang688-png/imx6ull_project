# Phase 6 知识点汇总（2026-06-02）

## devm_kzalloc 理解
- devm_kzalloc(&spi->dev, sizeof(*dev), GFP_KERNEL)
  - 第一个参数 &spi->dev 是"锚点"——标记这块内存归属于哪个设备
  - 内存是从内核通用堆（kmalloc）分配，不是从 SPI 设备里分配
  - devm_ 前缀 = 设备移除时自动 kfree，不用手动释放
  - 类比：devm_kzalloc(归属谁, 多大, 从哪要)

## spi_set_drvdata / i2c_set_clientdata
- 把私有数据指针存到总线设备结构体内部
- 目的：remove/suspend/resume 回调只收到 spi_device/i2c_client，不收到全局变量
- remove 中通过 spi_get_drvdata() / i2c_get_clientdata() 取回
- 全局变量 g_xxx 是给 open/read/ioctl 用的快捷方式（fops 收不到总线设备）

## SPI 核心概念
- spi_driver: 总线驱动结构体，probe(spi_device *), remove(spi_device *)
- spi_device: 包含 chip_select, max_speed_hz, mode（设备树提供）
- spi_transfer: 一段连续数据块（tx_buf/rx_buf + len）
- spi_message: transfer 链表，一个 CS 周期
- spi_sync: 同步执行整条 message（阻塞等待硬件完成）
- 读写协议：读 = 地址 | 0x80 (bit7=1), 写 = 地址 & 0x7F (bit7=0)

## I2C 核心概念
- i2c_driver: 总线驱动结构体，probe(client, id), remove(client)
- i2c_client: 包含 addr（0x1E）, adapter（控制器）
- i2c_smbus_read_byte_data: SMBus 单字节读（内核封装好的一行调用）
- i2c_msg + i2c_transfer: 底层多字节读（1 次 STOP，比多次 SMBus 快）

## SPI vs I2C 关键区别
- SPI 4 线全双工，I2C 2 线半双工
- SPI CS 片选寻址，I2C 广播地址寻址
- SPI DTS reg = <0>（CS号），I2C DTS reg = <0x1e>（从机地址）
- SPI probe 一个参数，I2C probe 两个参数
- SPI 无 SMBus 封装（4.1.15），I2C 有

## 大端 vs 小端
- ICM20608 大端：buf[0]=MSB, 解析 = (buf[0]<<8) | buf[1]
- AP3216C 小端：buf[0]=LSB, 解析 = (buf[1]<<8) | buf[0]

## CDEV 通用流程（与总线无关）
alloc_chrdev_region → cdev_init → cdev_add → class_create → device_create
I2C/SPI/platform 驱动完全一样

## 芯片验证
- ICM20608: 读 WHO_AM_I (0x75)，返回 0x98/0xAF
- AP3216C: 读 SYS_CONF (0x00)，上电默认 0x00
